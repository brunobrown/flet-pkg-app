# ============================================= [ DEFAULT ] ========================================================= #

[default]
FLET_PKG_APP_BASE_URL = "https://flet-pkg-app.onrender.com"

# --- APIs ---
GITHUB_API_BASE = "https://api.github.com"
PYPI_API_BASE = "https://pypi.org"
PYPISTATS_API_BASE = "https://pypistats.org/api"

# --- ClickHouse ---
CLICKHOUSE_URL = "https://sql-clickhouse.clickhouse.com"
CLICKHOUSE_USER = "demo"
CLICKHOUSE_PASSWORD = ""

# --- Flet Official ---
FLET_ORG = "flet-dev"
FLET_REPO = "flet"
FLET_PACKAGES_PATH = "sdk/python/packages"
FLET_OFFICIAL_PUBLISHER = "flet.dev"

# --- Cache ---
CACHE_TTL_SECONDS = 300
CACHE_TTL_DOWNLOADS = 86400
CACHE_TTL_DISCOVERY = 86400
CACHE_MAX_ENTRIES = 10000

# --- Local Index Cache (persistent JSON on disk) ---
LOCAL_INDEX_CACHE_TTL = 21600
LOCAL_INDEX_CACHE_DIR = ".cache"

# --- Index ---
INDEX_REINDEX_INTERVAL = 3600
INDEX_MAX_GITHUB_PAGES = 5
INDEX_GITHUB_PER_PAGE = 100
INDEX_DOWNLOAD_DAYS = 30

# --- Rate Limiting ---
PYPISTATS_MAX_CONCURRENT = 3
GITHUB_MAX_CONCURRENT = 10
MAX_RETRIES = 3

# --- Pagination ---
PAGE_SIZE_OPTIONS = [10, 25, 50, 100]
DEFAULT_PER_PAGE = 10

# --- New Package Threshold (days since creation) ---
NEW_PACKAGE_DAYS = 30

# --- Topics to exclude from card hashtags ---
EXCLUDED_TOPICS = ["flet", "python", "python3", "flet-dev"]

# --- Sort Options ---
SORT_OPTIONS = [
    "default ranking",
    "most stars",
    "most downloads",
    "recently updated",
    "newest package",
    "trending",
]

# --- Package Types ---
PACKAGE_TYPES = ["UI Controls", "Services"]

# --- Excluded Packages (core, not extensions) ---
EXCLUDED_PACKAGES = [
    "flet",
    "flet-cli",
    "flet-desktop",
    "flet-web",
    "flet-runtime",
    "flet-app-templates"
]

# --- Classification Keywords ---
SERVICE_KEYWORDS = [
    "service",
    "storage",
    "permission",
    "geolocation",
    "geolocator",
    "flashlight",
    "torch",
    "push notification",
    "onesignal",
    "ads",
    "audio record",
    "audio playback",
    "audio integration",
]

UI_CONTROL_KEYWORDS = [
    "control",
    "widget",
    "chart",
    "map",
    "video",
    "camera",
    "webview",
    "animation",
    "lottie",
    "rive",
    "table",
    "datatable",
    "editor",
    "code editor",
    "color picker",
]

# --- Category Filters ---
CATEGORIES = {gui = ["gui", "ui", "flet-gui", "interface", "flet-ui"], mobile = ["mobile", "android", "ios"], desktop = ["desktop", "desktop-app", "cross-platform"], web = ["web", "webapp", "fastapi", "flask"], devtools = ["flet-routing", "routing", "flet-architecture", "flet-design-pattern", "devtools"], database = ["sqlite", "database", "firebase", "supabase"], media = ["music-player", "audio", "video", "camera", "lottie", "rive"]}

# --- Ads (Google AdMob) ---
[default.ADS_BANNER]
# Test IDs — replace with real ad unit IDs in production
# Use Fixed-Size Banner IDs (not Adaptive) since BannerAd uses explicit width/height
android = "ca-app-pub-3940256099942544/6300978111"
ios = "ca-app-pub-3940256099942544/2435281174"

# --- Footer Links ---
[default.FOOTER_LINKS]
discord = "https://discord.gg/dzWXP8SHG8"
github = "https://github.com/flet-dev/flet"
blog = "https://flet.dev/blog/"
support = "https://flet.dev/support/"

# ============================================= [ DEVELOPMENT ] ====================================================== #

[development]

# ============================================= [ HOMOLOGATION ] ===================================================== #

[homologation]

# ============================================= [ PRODUCTION ] ======================================================= #

[production]

# ============================================= [ TESTING ] ========================================================== #

[testing]